<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% raw %}{{subject}}{% endraw %}</title>
  <style>
    /* Reset */
    body, p, h1, h2, h3 { margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
    
    /* Container */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
    }
    
    /* Header */
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 30px;
      text-align: center;
    }
    .header h1 {
      color: #ffffff;
      font-size: 24px;
      font-weight: 600;
    }
    .logo {
      max-height: 50px;
      margin-bottom: 10px;
    }
    
    /* Content */
    .content {
      padding: 40px 30px;
      background: #ffffff;
    }
    .content h2 {
      color: #1a1a2e;
      font-size: 22px;
      margin-bottom: 20px;
    }
    .content p {
      color: #4a4a4a;
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 20px;
    }
    
    /* Button */
    .btn {
      display: inline-block;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #ffffff !important;
      text-decoration: none;
      padding: 14px 30px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 16px;
      margin: 20px 0;
    }
    .btn:hover {
      opacity: 0.9;
    }
    
    /* Code Box */
    .code-box {
      background: #f4f4f4;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      padding: 20px;
      text-align: center;
      margin: 20px 0;
    }
    .code-box .code {
      font-family: 'Courier New', monospace;
      font-size: 32px;
      font-weight: bold;
      color: #667eea;
      letter-spacing: 5px;
    }
    
    /* Footer */
    .footer {
      background: #f9f9f9;
      padding: 30px;
      text-align: center;
      border-top: 1px solid #eee;
    }
    .footer p {
      color: #888;
      font-size: 13px;
      line-height: 1.5;
    }
    .footer a {
      color: #667eea;
      text-decoration: none;
    }
    
    /* Social */
    .social a {
      display: inline-block;
      margin: 0 8px;
    }
    .social img {
      width: 24px;
      height: 24px;
    }
    
    /* Utilities */
    .text-center { text-align: center; }
    .muted { color: #888; font-size: 14px; }
  </style>
</head>
<body style="background: #f0f0f0; padding: 20px;">
  <div class="email-container">
    <!-- Header -->
    <div class="header">
      {% raw %}{{#if logoUrl}}{% endraw %}
        <img src="{% raw %}{{logoUrl}}{% endraw %}" alt="{{ project.name }}" class="logo">
      {% raw %}{{else}}{% endraw %}
        <h1>{{ project.name }}</h1>
      {% raw %}{{/if}}{% endraw %}
    </div>
    
    <!-- Content -->
    <div class="content">
      {% raw %}{{{content}}}{% endraw %}
    </div>
    
    <!-- Footer -->
    <div class="footer">
      <p>
        © {% raw %}{{year}}{% endraw %} {{ project.name }}. All rights reserved.<br>
        <a href="{% raw %}{{unsubscribeUrl}}{% endraw %}">Unsubscribe</a> | 
        <a href="{% raw %}{{preferencesUrl}}{% endraw %}">Email Preferences</a>
      </p>
    </div>
  </div>
</body>
</html>
