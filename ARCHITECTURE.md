# 🔄 Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER (Client)                         │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              React + Vite Frontend                        │  │
│  │                                                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│  │  │  Step 1  │→│  Step 2  │→│   ...    │→│  Step 6  │ │  │
│  │  │ Project  │ │ Database │ │  Steps   │ │  Review  │ │  │
│  │  │  Setup   │ │  Config  │ │   3-5    │ │ Generate │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│  │                                              ↓            │  │
│  │                                      [Generate Button]   │  │
│  │                                              ↓            │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────────────────┬─────────────────────────────────┘
                                │ POST /api/generate
                                │ { config }
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      SERVER (Express)                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                  API Routes (routes.ts)                   │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  POST /api/generate                                 │  │  │
│  │  │    ↓                                                │  │  │
│  │  │  1. Validate with Zod Schema                       │  │  │
│  │  │    ↓                                                │  │  │
│  │  │  2. Call generator.generateProject(config)         │  │  │
│  │  │    ↓                                                │  │  │
│  │  │  3. Call zipGenerator.streamZip(files)             │  │  │
│  │  │    ↓                                                │  │  │
│  │  │  4. Stream ZIP to client                           │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Server Libraries (lib/)                      │  │
│  │                                                           │  │
│  │  ┌─────────────────┐  ┌──────────────┐  ┌────────────┐  │  │
│  │  │ Template        │  │  Generator   │  │    ZIP     │  │  │
│  │  │  Renderer       │→│   Service    │→│  Generator │  │  │
│  │  │                 │  │              │  │            │  │  │
│  │  │ • Nunjucks env  │  │ • Generate   │  │ • Archiver │  │  │
│  │  │ • Custom filters│  │   files      │  │ • Stream   │  │  │
│  │  │ • Render temps  │  │ • Format     │  │   to res   │  │  │
│  │  │                 │  │   w/Prettier │  │            │  │  │
│  │  └─────────────────┘  └──────────────┘  └────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                ↓                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │           Templates (templates/nestjs/)                   │  │
│  │                                                           │  │
│  │  • main.ts.njk          • tsconfig.json.njk              │  │
│  │  • app.module.ts.njk    • .eslintrc.js.njk               │  │
│  │  • app.controller.ts.njk • .prettierrc.njk               │  │
│  │  • app.service.ts.njk   • nest-cli.json.njk              │  │
│  │  • package.json.njk     • Dockerfile.njk                 │  │
│  │  • .env.example.njk     • docker-compose.yml.njk         │  │
│  │  • .gitignore.njk       • README.md.njk                  │  │
│  └───────────────────────────────────────────────────────────┘  │
└───────────────────────────────┬─────────────────────────────────┘
                                │ ZIP Stream
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENT DOWNLOADS                            │
│                                                                   │
│                    project-name.zip                              │
│                         ↓                                         │
│                    [Extract]                                     │
│                         ↓                                         │
│               Production-Ready NestJS Project                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Generation Flow

```
User Input (Steps 1-2)
        ↓
┌───────────────────┐
│   Zustand Store   │
│   (localStorage)  │
└─────────┬─────────┘
          │
          ↓
┌───────────────────────────────────────────┐
│         Step 6: Review & Generate         │
│                                           │
│  1. useEffect → Validate with Zod        │
│  2. Show errors if invalid               │
│  3. Enable/disable button                │
└─────────────────┬─────────────────────────┘
                  │ [User clicks Generate]
                  ↓
┌───────────────────────────────────────────┐
│      Frontend: generateProject()          │
│                                           │
│  1. setIsGenerating(true)                │
│  2. setProgress(10%)                     │
│  3. POST /api/generate                   │
│  4. setProgress(30%)                     │
└─────────────────┬─────────────────────────┘
                  │
                  ↓
┌───────────────────────────────────────────┐
│     Backend: /api/generate Handler        │
│                                           │
│  1. Validate body with Zod schema        │
│  2. Extract config data                  │
│  3. Log generation start                 │
└─────────────────┬─────────────────────────┘
                  │
                  ↓
┌───────────────────────────────────────────┐
│      generateProject(config)              │
│                                           │
│  For each template:                      │
│    1. renderTemplate(path, context)      │
│    2. formatCode(rendered, parser)       │
│    3. Add to files array                 │
│                                           │
│  Return: GeneratedFile[]                 │
└─────────────────┬─────────────────────────┘
                  │
                  ↓
┌───────────────────────────────────────────┐
│   streamZip(res, files, projectName)      │
│                                           │
│  1. Create archiver instance             │
│  2. Set response headers                 │
│  3. Pipe archive to response             │
│  4. For each file:                       │
│     - archive.append(content, name)      │
│  5. archive.finalize()                   │
└─────────────────┬─────────────────────────┘
                  │
                  ↓
┌───────────────────────────────────────────┐
│      Frontend: Handle Response            │
│                                           │
│  1. setProgress(60%)                     │
│  2. Get blob from response               │
│  3. setProgress(80%)                     │
│  4. Create download link                 │
│  5. Trigger download                     │
│  6. setProgress(100%)                    │
│  7. Show success toast                   │
│  8. setIsComplete(true)                  │
└───────────────────────────────────────────┘
```

---

## Template Rendering Pipeline

```
Configuration Object
        ↓
┌─────────────────────┐
│  TemplateContext    │
│  {                  │
│    projectName,     │
│    description,     │
│    author,          │
│    license,         │
│    nodeVersion,     │
│    packageManager,  │
│    databaseType,    │
│    provider,        │
│    connectionString,│
│    autoMigration    │
│  }                  │
└──────────┬──────────┘
           │
           ↓
┌────────────────────────────────────┐
│  Nunjucks Environment              │
│                                    │
│  • Templates path configured       │
│  • Custom filters added:           │
│    - lower                         │
│    - upper                         │
│    - replace                       │
└──────────┬─────────────────────────┘
           │
           ↓
┌────────────────────────────────────┐
│  Template Files (.njk)             │
│                                    │
│  Variables: {{ projectName }}      │
│  Conditionals: {% if ... %}        │
│  Filters: {{ text | lower }}       │
└──────────┬─────────────────────────┘
           │
           ↓
┌────────────────────────────────────┐
│  Rendered Code (Raw)               │
│                                    │
│  TypeScript, JSON, YAML, etc.      │
└──────────┬─────────────────────────┘
           │
           ↓
┌────────────────────────────────────┐
│  Prettier Formatting               │
│                                    │
│  • Parser detection by file type   │
│  • Apply formatting rules          │
│  • Handle errors gracefully        │
└──────────┬─────────────────────────┘
           │
           ↓
┌────────────────────────────────────┐
│  Final Formatted Code              │
│                                    │
│  Ready for ZIP packaging           │
└────────────────────────────────────┘
```

---

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     USER INTERFACE                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │  Form Inputs    │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │ Zustand Store   │
                    │ + localStorage  │
                    └────────┬────────┘
                             ↓
┌──────────────────────────────────────────────────────────────┐
│                   Configuration Object                        │
│  {                                                           │
│    projectSetup: {                                           │
│      projectName: "my-app",                                  │
│      description: "...",                                     │
│      author: "...",                                          │
│      license: "MIT",                                         │
│      nodeVersion: "20",                                      │
│      packageManager: "npm"                                   │
│    },                                                        │
│    databaseConfig: {                                         │
│      databaseType: "PostgreSQL",                             │
│      provider: "Neon",                                       │
│      connectionString: "postgresql://...",                   │
│      autoMigration: "push"                                   │
│    },                                                        │
│    modelDefinition: {...},    // Sprint 2                    │
│    authConfig: {...},          // Sprint 3                    │
│    featureSelection: {...}     // Sprint 4                    │
│  }                                                           │
└──────────────────────────────┬───────────────────────────────┘
                               │
                  ┌────────────┴────────────┐
                  │                         │
         ┌────────▼────────┐      ┌────────▼────────┐
         │  Zod Validation │      │   API Request   │
         │   (Frontend)    │      │  (POST /api/)   │
         └─────────────────┘      └────────┬────────┘
                                           │
                                  ┌────────▼────────┐
                                  │ Zod Validation  │
                                  │   (Backend)     │
                                  └────────┬────────┘
                                           │
                                  ┌────────▼────────┐
                                  │  Template       │
                                  │  Rendering      │
                                  └────────┬────────┘
                                           │
                                  ┌────────▼────────┐
                                  │  Code Files     │
                                  │  (14 files)     │
                                  └────────┬────────┘
                                           │
                                  ┌────────▼────────┐
                                  │  ZIP Package    │
                                  └────────┬────────┘
                                           │
                                  ┌────────▼────────┐
                                  │  User Download  │
                                  └─────────────────┘
```

---

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────┐
│                        Error Sources                          │
└──────────────────────────────────────────────────────────────┘
     ↓                    ↓                    ↓
┌──────────┐    ┌─────────────────┐    ┌─────────────────┐
│ Frontend │    │    Backend      │    │   Template      │
│  Input   │    │   Validation    │    │   Rendering     │
└────┬─────┘    └────────┬────────┘    └────────┬────────┘
     │                   │                       │
     ↓                   ↓                       ↓
┌──────────────────────────────────────────────────────────────┐
│                     Error Handlers                            │
└──────────────────────────────────────────────────────────────┘

Frontend Validation Errors:
• Project name invalid regex → Show inline error
• Connection string empty → Show inline error
• Steps 1-2 incomplete → Disable generate button + show alert

Backend Validation Errors:
• Zod parse fails → Return 400 + error details
• Required fields missing → Return 400 + field list

Generation Errors:
• Template not found → Log + return 500
• Prettier fails → Use unformatted code + log warning
• Archive fails → Return 500 + error message

Response Handling:
• response.ok === false → Show error toast with message
• Network error → Show "Network error" toast
• Success → Show success toast + download file
```

---

## State Management

```
┌────────────────────────────────────────────────────────────┐
│                    Zustand Store                            │
│                                                            │
│  State:                                                    │
│    • currentStep: number (1-6)                            │
│    • config: Partial<WizardConfig>                        │
│    • isStepValid: (step) => boolean                       │
│                                                            │
│  Actions:                                                  │
│    • nextStep()                                           │
│    • previousStep()                                       │
│    • goToStep(n)                                          │
│    • updateProjectSetup(data)                             │
│    • updateDatabaseConfig(data)                            │
│    • updateModelDefinition(data)                          │
│    • updateAuthConfig(data)                               │
│    • updateFeatureSelection(data)                         │
│    • resetWizard()                                        │
│                                                            │
│  Persistence:                                             │
│    • localStorage key: "wizard-storage"                   │
│    • Auto-saves on every update                           │
│    • Restores on page load                                │
└────────────────────────────────────────────────────────────┘
```

---

## API Contract

### POST /api/generate

**Request:**

```json
{
  "projectSetup": {
    "projectName": "string (kebab-case)",
    "description": "string",
    "author": "string",
    "license": "MIT" | "Apache-2.0" | "GPL-3.0" | "BSD-3-Clause" | "ISC",
    "nodeVersion": "18" | "20" | "22",
    "packageManager": "npm" | "yarn" | "pnpm"
  },
  "databaseConfig": {
    "databaseType": "PostgreSQL" | "MySQL" | "MongoDB",
    "provider": "Neon" | "Supabase" | "Atlas" | "PlanetScale" | "Railway",
    "connectionString": "string",
    "autoMigration": "push" | "manual"
  }
}
```

**Response (Success - 200):**

- Content-Type: `application/zip`
- Content-Disposition: `attachment; filename="project-name.zip"`
- Body: Binary ZIP stream

**Response (Validation Error - 400):**

```json
{
  "error": "Invalid configuration",
  "details": [
    {
      "path": ["projectSetup", "projectName"],
      "message": "Use lowercase letters, numbers, and hyphens only"
    }
  ]
}
```

**Response (Server Error - 500):**

```json
{
  "error": "Failed to generate project",
  "message": "Template rendering failed"
}
```

---

## Component Hierarchy

```
App
└── Wizard
    ├── Step1ProjectSetup
    │   ├── WizardLayout
    │   ├── InputField (projectName)
    │   ├── InputField (description)
    │   ├── InputField (author)
    │   ├── SelectField (license)
    │   ├── SelectField (nodeVersion)
    │   └── ToggleField (packageManager)
    │
    ├── Step2DatabaseConfig
    │   ├── WizardLayout
    │   ├── SelectField (databaseType)
    │   ├── SelectField (provider)
    │   ├── Input (connectionString with password toggle)
    │   └── ToggleGroup (autoMigration)
    │
    ├── Step3ModelBuilder (UI only - Sprint 2)
    ├── Step4AuthSetup (UI only - Sprint 3)
    ├── Step5FeatureSelection (UI only - Sprint 4)
    │
    └── Step6Review
        ├── WizardLayout
        ├── Alert (validation errors)
        ├── Card[] (section summaries)
        ├── SyntaxHighlighter (JSON preview)
        ├── Button (copy, download config)
        ├── Button (generate - calls API)
        ├── Progress (generation progress)
        └── Card (success state)
```

This architecture ensures clean separation of concerns, easy testing, and maintainability for future sprints! 🚀
